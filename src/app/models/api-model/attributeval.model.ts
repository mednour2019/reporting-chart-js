export interface attributeVal{
  text:string,
  textDimParent:string,
  expanded:boolean,
  selected: boolean,
  items: valList[]
}
export interface valList{
  text:string,
  originalText:string,
  selected: boolean

}
